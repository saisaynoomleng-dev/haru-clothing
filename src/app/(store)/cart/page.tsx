import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Cart',
  description: `You're one step away from elevated living. Review the items in your Haru cart and proceed to our secure checkout to complete your order.`,
};

const CartPage = () => {
  return <div>CartPage</div>;
};

export default CartPage;
